"use client";

// This demo has no weather API key configured, so rather than show fake
// placeholder data as if it were real, the widget simply renders nothing.
export default function WeatherWidget() {
  return null;
}
